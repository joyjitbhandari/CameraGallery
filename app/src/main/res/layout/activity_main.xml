<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/rlButtons"
        android:layout_centerHorizontal="true"
        >
        <com.google.android.material.button.MaterialButton
            android:id="@+id/img_gallery_btn"
            android:layout_width="wrap_content"
            android:layout_height="71dp"
            android:text="Photos"
            app:cornerRadius="12dp"
            android:layout_margin="8dp"
            app:icon="@drawable/ic_photo_library"
            app:layout_constraintBottom_toBottomOf="@+id/vid_gallery_btn" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/vid_gallery_btn"
            android:layout_width="wrap_content"
            android:layout_height="71dp"
            android:text="Videos"
            app:cornerRadius="12dp"
            app:icon="@drawable/ic_video_library"
            android:layout_margin="8dp"
            android:layout_toEndOf="@+id/img_gallery_btn"
            />

        <Button
            android:id="@+id/btn_camera"
            android:layout_width="wrap_content"
            android:layout_height="71dp"
            android:text="camera"
            android:layout_below="@+id/img_gallery_btn"
            app:cornerRadius="12dp"
            android:layout_margin="8dp"
            app:icon="@drawable/ic_camera"
            app:layout_constraintEnd_toEndOf="@+id/img_gallery_btn"
            app:layout_constraintHorizontal_bias="0.117"
            app:layout_constraintStart_toStartOf="@+id/img_gallery_btn"
            app:layout_constraintTop_toBottomOf="@+id/img_gallery_btn" />

        <Button
            android:id="@+id/btn_videocam"
            android:layout_width="wrap_content"
            android:layout_margin="8dp"
            android:layout_height="71dp"
            android:text="Record"
            android:layout_below="@id/img_gallery_btn"
            android:layout_toEndOf="@id/btn_camera"
            app:cornerRadius="12dp"
            app:icon="@drawable/ic_camera"
            app:layout_constraintEnd_toEndOf="@+id/vid_gallery_btn"
            app:layout_constraintHorizontal_bias="0.943"
            app:layout_constraintStart_toStartOf="@+id/vid_gallery_btn"
            tools:layout_editor_absoluteY="71dp" />
    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="369dp"
        android:layout_height="555dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:layout_below="@+id/rlButtons"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/itemview">
    </androidx.recyclerview.widget.RecyclerView>

</RelativeLayout>